<Window x:Class="OperatorModule.Views.DeviationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OperatorModule.Views"
        mc:Ignorable="d"
       Title="Сообщение об отклонении"
        Height="350" Width="450"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Сообщение об отклонении в процессе" 
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Параметр:" Margin="0,0,0,5"/>
        <TextBox x:Name="ParameterBox" Grid.Row="2" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Text="Описание отклонения:" Margin="0,0,0,5"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="4" TextWrapping="Wrap" 
                 AcceptsReturn="True" Height="100" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="Отправить" Width="100" Margin="5"
                    Background="#E74C3C" Foreground="White" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="80" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
